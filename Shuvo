import os
import platform
import sys

def get_arch():
    machine = platform.machine().lower()
    if "aarch64" in machine or "arm64" in machine:
        return "64"
    return "32"

# আপনার চ্যানেল ওপেন করার ফাংশন
def open_link():
    os.system("termux-open-url https://t.me/tem_with")

arch_type = get_arch()

if arch_type == "64":
    try:
        import HBXUSER
        # আপনার তথ্য যোগ করা হলো
        os.system("clear")
        open_link()
        print("\033[1;32m[+] DEVELOPER: @shuvoth1")
        print("[+] CHANNEL  : @tem_with\033[0m")
        
        HBXUSER.professional_logo()
        HBXUSER.bot.infinity_polling()
    except ImportError:
        print("❌ Error: HBXUSER.so missing!")
        sys.exit()
    except Exception as e:
        print(f"Error: {e}")
else:
    print("❌ ৩২-বিট ফোনে এটি চলবে না।")
    sys.exit()